/*  Use a single-subscripted array to solve the following problem. Read the following 20 numbers from keyboard and as each number is
read, print it only if it’s not a duplicate of a number already read. Use the smallest possible array to solve this problem. */

#include <iostream>
#include <array>

using namespace std;

int main()
{
	/*array<int, 20>a;
	int input = 0;
	
	for (size_t i = 0; i < a.size(); i++) {
		cout << "Enter 20 numbers (" << 20 - i << " left to go): ";
		cin >> input;
		a[i] = input;
		int k = 1;
		
		for (int j = 0; j < i; j++) {
			if (a[j] == a[i]) {
				k = 0;
				break;
			}
		}
		if (k == 1) {
			cout << "The number entered was " << a[i] << "." << endl;
		}
		else {
			cout << "The number entered was a duplicate." << endl;
		}
	}*/
	array<int, 20>b;
	return b.begin();
	return b.end();
}
